# Silver layer
#### In this layer the data loaded (full load, truncate and insert) from the bronze layer to the silver layer for implementing the data transformation and inserting the cleaned data to the employees table
